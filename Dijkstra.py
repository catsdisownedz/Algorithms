from rich.console import Console
from rich.live import Live
from rich.table import Table
from queue import PriorityQueue
import random
import time

#dijkstra is considered the stupider A* algorithm, since it calculates the nearest small distance without 
#considering the distance to the destination (its also called the uniform cost search)

#men el a5er a blind algorithm(uninformed)
class Dijkstra:
    def __init__(self, console):
        self.console = console
        
        
    def amazing_algorithm(self):
        pass
        
        
        